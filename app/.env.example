# Optional: Backend API URL (for production deployment on Vercel)
# If not set, defaults to the current site origin + /api/public.
# Use this when the frontend and backend are deployed on different domains.
VITE_API_URL=https://your-backend.vercel.app
